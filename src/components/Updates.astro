---
const updates = [
    {
        date: "2024-12-17",
        title: "First YouTube content up",
        content: "My first YouTube tutorial video is out. I'm still practicing, but have a look at how to get started with Databricks on AWS using their free trials.",
        link: "https://medium.com/@nicolaj.schmit"
    },
    {
        date: "2024-12-17",
        title: "The GitHub static site is up",
        content: "The base template was created in 10 minutes using https://bolt.new/ including subsites, layout and GitHub deployment pipelines.",
        link: "https://bolt.new/"
    }
];
---
<section id="updates" class="section-container">
    <h2>Latest Updates</h2>
    <div class="updates-grid">
        {updates.map(update => (
            <article class="update-card">
                <time>{update.date}</time>
                <h3>{update.title}</h3>
                <p>{update.content}</p>
                {update.link && (
                    <a href={update.link} target="_blank" rel="noopener noreferrer">Read more →</a>
                )}
            </article>
        ))}
    </div>
</section>

<style>
    .updates-grid {
        display: grid;
        gap: 2rem;
        margin-top: 2rem;
    }
    .update-card {
        background: var(--bg-light);
        padding: 1.5rem;
        border-radius: 8px;
        border: 1px solid rgba(148, 163, 184, 0.1);
    }
    time {
        color: var(--text-muted);
        font-size: 0.9rem;
    }
    h3 {
        color: var(--primary-light);
        margin: 0.5rem 0;
    }
    a {
        display: inline-block;
        margin-top: 1rem;
        color: var(--primary);
        text-decoration: none;
        transition: color 0.3s ease;
    }
    a:hover {
        color: var(--primary-light);
    }
</style>